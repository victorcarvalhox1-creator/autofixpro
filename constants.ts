import { OSStatus } from './types';

export const KANBAN_COLUMNS = [
  OSStatus.DESMONTAGEM,
  OSStatus.FUNILARIA,
  OSStatus.PREPARACAO,
  OSStatus.PINTURA,
  OSStatus.MONTAGEM,
  OSStatus.POLIMENTO,
  OSStatus.FINALIZADO
];